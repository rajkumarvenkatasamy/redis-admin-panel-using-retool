# You should be in redis container terminal before executing these commands
# Login to redis container terminal using the below command:
# docker exec -it redis bash
# from the machine where redis container is running
# You can also login to redis container terminal using Docker Desktop

redis-cli -h localhost -p 6379 < users.redis